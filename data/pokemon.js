/**
 * Pokemon Roster (data)
 * -----------------------
 * Seed roster data. Currently empty -- the app starts with zero Pokemon
 * and everything is added through the UI.
 */

const ROSTER_DATA = [];
