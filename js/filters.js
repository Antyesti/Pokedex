/* ============== FILTER/SEARCH HOOKS ============== */
searchInput.addEventListener('input', renderGrid);
typeFilter.addEventListener('change', renderGrid);
gameFilter.addEventListener('change', renderGrid);
sortFilter.addEventListener('change', renderGrid);

